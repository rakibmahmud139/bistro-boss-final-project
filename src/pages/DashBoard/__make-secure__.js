/**
 * ---------------
 *      BASIC
 * ---------------
 * 1. don't show link to them who shouldn't see the link
 *  only show to the person/types of user who should see it
 * 3. don't allow to visit the link by typing on the url
 * 
 * 
 * -----------------------
 *      TO SEND DATA
 * -----------------------
 * 1. Verify JWT token(send authorization token in the header to the server if possible use axios to send jwt token by intercepting the request)
 * 2. If is an admin activity . make sure only admin user is posting data by using verify admin
*/